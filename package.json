{
	"name": "react-dataflow-editor-demo",
	"private": true,
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build-editable": "esbuild editable.tsx --bundle --minify --outfile=editable.min.js",
		"build-readonly": "esbuild readonly.tsx --bundle --minify --outfile=readonly.min.js",
		"build": "npm run build-readonly && npm run build-editable"
	},
	"dependencies": {
		"@types/react": "^17.0.19",
		"@types/react-dom": "^17.0.9",
		"esbuild": "^0.12.23",
		"react": "^17.0.2",
		"react-dataflow-editor": "^0.1.2",
		"react-dom": "^17.0.2"
	}
}
